import { styled } from "@/styled-system/jsx";

export const FormStyled = styled("form", {
  base: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    gap: "20px",
  },
});
